/**
 * OBSOLETE as of Phase 3.
 *
 * This file was the in-memory "database" for Phase 1. The live app now talks
 * to FastAPI + PostgreSQL via `mocks/api.ts` (Axios). Kept only so you can
 * compare fake vs real data shapes while learning — safe to delete later.
 */
export {}
